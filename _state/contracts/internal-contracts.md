# `@internal` contract ledger (SEED — hand-authored, not generated)

Will be generated by `comparison:architecture:internal-contracts --write` once the audit command is ported
(implementation plan Phase 7). Until the bundle has a `src/`, there is nothing to scan.

0 distinct symbols across 0 occurrences — **0 tracked**, **0 untracked**.

| Symbol | Kind | Used in | Tracked in Appendix D |
|---|---|---|---|
| _(none yet — no `src/` to scan)_ | — | — | — |

## Anticipated `@internal` contracts (not yet in code)

The implementation (base-idea §6) is expected to touch these upstream namespaces, whose `@internal` symbols
the audit will track once code exists:

- `Pimcore\Bundle\StudioBackendBundle\Security\Service\SecurityServiceInterface` — element/user permission checks
- `Pimcore\Bundle\StudioBackendBundle\DataObject\Service\LayoutServiceInterface` — per-user layout resolution
- `Pimcore\Bundle\StudioBackendBundle\Controller\AbstractApiController` — REST base class
- `Pimcore\Bundle\StudioBackendBundle\Exception\Api\*` — ProblemDetails-style API errors
- `Pimcore\Bundle\StudioUiBundle\Webpack\WebpackEntryPointProviderInterface` — frontend entry-point registration

(`Pimcore\Model\DataObject\ClassDefinition\Data`'s diff API — `getDiffDataForEditMode`, `getVersionPreview`,
`EqualComparisonInterface::isEqual` — is the load-bearing reuse but is public API, not `@internal`.)

## Service-id strings

None yet. Studio-UI DI string ids (if any are introduced) will be enumerated in the audit's
`WATCHED_SERVICE_IDS` once the frontend exists.
